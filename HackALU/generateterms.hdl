/*Generates generate terms*/
/*Used for 4 bit CLA without higher level terms*/
CHIP generateterms{
  IN X[4],Y[4];
  OUT G[4];
  PARTS:
  And(a=X[0],b=Y[0],out=G[0]);
  And(a=X[1],b=Y[1],out=G[1]);
  And(a=X[2],b=Y[2],out=G[2]);
  And(a=X[3],b=Y[3],out=G[3]);
}
